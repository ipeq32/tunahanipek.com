import { PageLoading } from '@/components/layout/page-loading';

export default function Loading() {
  return (
    <main className="mesh-background min-h-[50vh]">
      <PageLoading />
    </main>
  );
}
