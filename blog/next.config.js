/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      // {
      //   protocol: 'https',
      //   hostname: 'www.instagram.com',
      //   port: '',
      //   pathname: '/p/**',
      // },
    ],
  },
}

module.exports = nextConfig
